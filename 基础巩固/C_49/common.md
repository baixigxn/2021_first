# 题目：

海滩上有一堆桃子，五只猴子来分。第一只猴子把这堆桃子平均分为
	五份，多了一个，这只猴子把多的一个仍入大海中，拿走了一份。第二只猴
	子把剩下的桃子又平均分成五份，又多了一个，它同样把多的一个扔入大海，
	拿走了一份，第三，第四，第五只猴子都是这样做的，问海滩上原来最少有
	多少个桃子？


### 程序分析

- 最少的可能就是剩下四个桃子



### 主要代码

main.c

```c
int main(void){
    int i,j,m,k,count;
    for(i=4;i<10000;i+=4){
        m=i;//m表示猴子拿走后剩余的桃子数目
        count=1;//用count来限制k(猴子数目)的增加
        for(k=0;k<5;k++){//for循环控制一个猴子进行的操作
            j=m/4*5+1;//j表示桃子的数目
            if(j%4==0)
                count++;
            else
                break;
        }
        if(count==5){
            printf("至少有的桃子数：%d",j);
        }
    }
    system("pause");
    return 0;
}
```



### 今日巩固

1. 这个题主要是找到剩余桃子m和上一个猴子在的时候留下来的桃子数目m/4*5+1之间的关系

2. 还有就是通过count来控制桃子不是整数的情况

   



